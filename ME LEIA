Usei o docker para utilizar o postgre, usei o comando 
"docker run --name some-postgres -p 5432:5432 -e POSTGRES_PASSWORD=password -d postgres"
Database = "docker exec -it some-postgres /bin/bash" | "psql -U postgres" | "create database postodb;"

python manage.py createsuperuser --> adm/senha=adm123 -->  http://127.0.0.1:8000/admin/ --> Inserir Dados/Criação de usuarios

http://127.0.0.1:8000/layout/ --> pagina inicial

Ambiente virtual --> 'python -m venv env' | 'env/scripts/activate'


Requisitos:
pip install django
pip install psycopg2
pip install django-environ
